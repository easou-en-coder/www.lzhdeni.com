

请重写 sitemap.xml，规则如下：
1. 只收录当前项目目录及其子目录下的 HTML 页面。
2. 排除所有 404.html、50x.html 页面，不允许出现在 sitemap 中。
3. 在生成的 sitemap URL 中：
   - 去掉所有 .html 后缀；
   - 去掉 index.html，使目录路径（/path/）直接作为页面 URL。
4. URL 必须以 https://lzhdeni.com/ 开头。
5. 不增加 lastmod 字段
6. 保证生成的 sitemap.xml 符合 sitemap XML 标准格式。
7. 不要包含 / 以外的任何路径。
8. 最后用中文简要说明你做了哪些处理以及生成结果的特征。





重新帮我检查 / 目录下 所有html页面，是否有代码错误、css错误、javascript 错误？
是否有超链失效的？是否有图片缺失的？是否有外链失效的？以及是否
有图片未被引用的？所有页面的<head></head>部分是否有错误？
图片链接是否失效？以及还有哪些问题的，都可以列出来；最后用中文给我结果



再继续从谷歌收录、深度SEO、用户体验UX 角度分析 sitemap.xml ：


